signin
    .container
        .row
            .col-sm-6.col-md-4.col-md-offset-4
                .account-wall
                    #my-tab-content.tab-content
                        #login.tab-pane.active
                            p=connectionMessage
                            form.form-signin(action='/login', method='post')
                                input.form-control(type='text' name='username' placeholder='Username', required=true, autofocus='')
                                input.form-control(type='password' name='password' placeholder='Password', required=true)
                                input.btn.btn-lg.btn-default.btn-block(type='submit', value='Sign In', onClick='findByUsername()')
                            #tabs(data-tabs='tabs2')
                                p.text-center
                                    a(href='#register', data-toggle='tab') Need an Account?
                        #register.tab-pane
                            p=connectionMessage
                            form.form-signin(action='/register', method='post')
                                input.form-control(type='text' name='newUser' placeholder='User Name ...', required=true, autofocus='')
                                input.form-control(type='password' name='newPass' placeholder='Password ...', required=true)
                                input.btn.btn-lg.btn-default.btn-block(type='submit', value='Sign Up')
                            #tabs2(data-tabs='tabs')
                                p.text-center
                                    a(href='#login', data-toggle='tab') Have an Account?

signup
